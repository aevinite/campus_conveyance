import { RouteNotFound } from '@/components/route-not-found';

export default function InstitutionPanelNotFound() {
  return <RouteNotFound homeHref="/institution" homeLabel="Back to dashboard" />;
}
