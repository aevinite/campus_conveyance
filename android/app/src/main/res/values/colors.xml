<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Ink backdrop shared by the launch splash, the native Capacitor splash
         (capacitor.config.ts) and the web boot loader (app-splash.tsx), so the
         hand-off from launch → WebView never flashes white. -->
    <color name="splash_background">#1c1917</color>
</resources>
