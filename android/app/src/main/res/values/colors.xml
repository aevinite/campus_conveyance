<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Light warm-white backdrop shared by the launch splash and the native
         Capacitor splash (capacitor.config.ts). The app opens in light mode by
         default, so the launch surface matches the web boot loader (app-splash
         .tsx) and there is no dark flash before the WebView paints. -->
    <color name="splash_background">#fbfaf6</color>
</resources>
