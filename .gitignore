# Dependencies
node_modules/

# Next.js build output
.next/
out/
build/
tsconfig.tsbuildinfo

# Env files — never commit secrets; keep the example template.
.env*
!.env.example

# Local dev/server logs — runtime artifacts, never committed.
*.log

# OS / editor / tooling
.DS_Store
.pdfpages/
.maintenance.json
.claude/

# Vercel
.vercel

# Local E2E test scripts + Playwright storage-state (contains auth tokens) —
# never commit; these hold live session tokens and test-account passwords.
_e2e/
